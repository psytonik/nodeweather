export const getArgs = (args) => {    const res = {};    const [exec,file,...rest] = args;    rest.forEach((value, ind, arr)=>{        // check for first char -        if(value.charAt(0) === '-'){            if(ind === arr.length -1){                // 1. if found give him true                res[value.substring(1)] = true;                // 2. if not found go to next            }else if(arr[ind+1].charAt(0) !== '-'){                res[value.substring(1)] = arr[ind +1]            } else {                // 3. if nothing give him true                res[value.substring(1)] = true;            }        }    })    return res;}